#include <stdio.h>

int main() {
    int T;
    scanf("%d", &T);

    int N, L;
    long long int M;
    scanf("%lld %d %d", &N, &L, &M);

    M += M * (L / 100 / 12) * (80/100);
    printf("")
    return 0;
}